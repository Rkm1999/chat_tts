# Python 3.11.9
# GPU: RTX 30xx (Ampere, sm86) — CUDA 12.4

# Core audio / ML
numpy>=1.26.0
scipy==1.15.3
sounddevice==0.5.1
soundfile==0.12.1
tokenizers==0.22.2
onnxruntime-gpu

# TTS
faster-qwen3-tts==0.2.5
qwen-tts==0.1.1
# qwen3-tts-triton declares python_requires>=3.12 (we're on 3.11); install manually:
#   pip install qwen3-tts-triton==0.1.0 --ignore-requires-python --no-deps

# Triton — bundled with PyTorch on Linux/WSL; listed here to make the dep explicit
# PyTorch 2.6 (cu124) ships Triton 3.2
triton
torchao==0.9.0
# PyTorch — must be installed separately with CUDA support:
#   pip install torch==2.6.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchaudio==2.6.0

# Discord bot
discord.py[voice]
python-dotenv==1.1.0

